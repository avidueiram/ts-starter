# Commit

Commit changes with contextual messages, following `.cursor/rules/commit.mdc`.

Always include files in .cursor if modified.

Commit dummy secrets only if absolutely certain. Ask for confirmation when unsure.
